import React from 'react';

const Roles = ({display}) => {

    return (
        <div>
            <input 
                    type="text"
                    // onChange={handleChange}
                    // value={text}
                    placeholder="Job titles"
                    />  
        </div>
    )
}

export default Roles;
